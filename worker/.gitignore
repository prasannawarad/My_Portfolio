.dev.vars
node_modules

# Secrets & local dev
.env
.env.*
.dev.vars

# Local miniflare/wrangler state. Path is relative to this file, so no `worker/`
# prefix — that pattern silently matched nothing and let the cache get committed.
.wrangler/
